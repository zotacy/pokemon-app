<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/app.css">
    <title>Pokédex Entry</title>
</head>
    <body>
        <div class='pokedex'>
            <h1 class='pdex'>Who's that Pokémon?</h1>
            <div id="newEntryScreen">
                <!-- injecting data -->
                <h2><%= pokemon.name %></h2>
                <img class= "image" src="<%=pokemon.img%>" alt="pokemon.img">
                <a href="<%= pokemon.img %>"></a>
                <br><br>
            <!-- </div>
            <div> -->
                <a href="/pokemon/<%=pokemon.id; %>/edit">Edit <%= pokemon.name %>'s Entry</a>

                <form action="/pokemon/<%=pokemon.id %>?_method=DELETE" method="POST">
                    <input type="submit" value="DELETE <%= pokemon.name %>" />
                </form>
            </div>
        </div>
    </body>
</html>